# LinkHub Frontend environment variables
# Replace the placeholder values with your real values before running the app

# Backend API URL (no trailing slash)
REACT_APP_BACKEND_URL=http://localhost:4500

# Note: In Create React App, only variables that start with REACT_APP_ will be exposed to your application