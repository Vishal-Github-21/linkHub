# LinkHub Backend environment variables
# Replace the placeholder values with your real credentials before running the app.
# IMPORTANT: Do not commit this file to version control. Add it to .gitignore.

# MongoDB connection string (e.g. mongodb://username:password@host:port/dbname)
MONGO_URI="mongodb+srv://vishalbackup690_db_user:vishalbackup690_db_user@cluster0.qvh6rny.mongodb.net/?appName=Cluster0"

# Gmail account used to send emails (or an app-specific password)
EMAIL_USER="vishalbackup690@gmail.com"
EMAIL_PASSWORD="ypugszytkgmitnyc"

# Frontend URL used in emails and links (no trailing slash)
Frontend_URL=http://localhost:3000

# JWT secret used by authentication (keep this secret, use a long random string)
JWT_SECRET="vishal_212121"

# Optional: server port (the app currently uses 4500 in code)
PORT=4500
